# Audit, critique and polish · 13 September 2026

The cleanup of the Health OS design system in place, following Tumai's Codex interview (`health-os-ds-rebuild/DECISIONS.md`), Tumai's six section edits, and the Impeccable and Taste skills. This report records what changed, what the skills flagged, what was fixed, and what was kept on purpose.

Open decisions are not repeated here. They live in the catalogue and are listed at the top of [REFERENCE.md](../REFERENCE.md#open-decisions) and in the Checklist section of the site.

## 1. Tumai's section edits

| # | Edit | Result |
|---|---|---|
| 1 | Preferred gradients: signature, soft wash, warm sunrise | The three gradient tokens, shown in Tokens with their use and limits. Every other gradient and all seven glow tokens are gone |
| 2 | Delete all icons and leave a blank space | The Icons section is an empty panel marked in progress. The squircle icon embed is deleted |
| 3 | Remove every motion button, including celebrate and sign up | Celebration, confetti, glow, magnetize and save buttons deleted. `Button` is primary, secondary and text, in two sizes, with loading and disabled states; `IconButton` is separate |
| 4 | Keep all badges | All eight badge groups kept, recoloured onto the three-shade palette |
| 5 | Keep all widgets, animate them, remove 32 · Inline alert and "Using this library" | 31 widgets rebuilt as React components that animate as they scroll into view and take documented inputs. Widget 32 and the note are gone, and so is the iframe embed they lived in |
| 6 | Remove the memoji section entirely | The section is deleted. It had no separate data or image files |

## 2. The interview decisions, as built

| Decision | In the system |
|---|---|
| Clean in place, AI agents first | Same repository and Vercel project. `llms.txt`, `CLAUDE.md`, `REFERENCE.md` and `CHECKLIST.md` form the agent path; the site is the human path |
| One source, docs in the same pass | `tokens.json` generates the CSS variables, the Tailwind preset and the JS palette. `catalog.json` feeds the site's usage panels and generates the written reference. `npm run build` fails when either is stale |
| Roughly 80% fewer choices | Token values from 203 to 69 (80 counting the eleven documented spacing steps, which equal Tailwind's defaults). Colours 78 to 26, type sizes 14 to 4 roles, radii 9 to 4, shadows 7 to 3, gradients 4 to 3, glows 7 to 0, themes 3 to 2 |
| Light and paper only | `.dark` and every `dark:` class removed; the lint rejects `dark:` |
| Three primaries, three shades | Rose, apricot and lavender at 50, 400 and 700. Neutrals and status colours sit apart |
| Anonymous Pro body, three roles plus labels | `text-heading`, `text-subheading`, `text-body`, `text-label`, each with one weight |
| 8px rhythm, 4px compact steps | Documented scale; the lint rejects half steps |
| Three button styles, neutral primary allowed | `variant` primary, secondary or text; `tone` brand or neutral |
| Six independently styled cards | Content, feature, service, pricing, resource and action, each its own component |
| Full functional widget library | 31 widgets, five labelled experimental and on by default, inputs listed beside each |
| All imagery kept and tagged | 169 images with two or three context tags, palette, tone, text space, a description and one suggested use. Nothing deleted |
| Open-ended motion, on by default | Motion library kept; the four previously deprecated pieces are labelled experimental instead |
| Pre-asset checklist | 13 checks, interactive on the site and generated into `CHECKLIST.md` |
| No contrast gate, claims stay with the database | No contrast gate added. Prices come from the database; testimonials, the stat band and competitor names are gone |

## 3. Impeccable

Ran the Impeccable 2.1.7 anti-pattern detector over `src/` and the production build, then worked through its critique, audit and polish guidance.

| Finding | Where | Action |
|---|---|---|
| Gradient text | `GradientShimmer` used `background-clip: text` | Fixed. The text is one solid tonal neutral with a moving mask band. Headline emphasis uses `.text-highlight`, a gradient behind the word |
| Layout property animation | Reference rail animated `width`; main column animated `padding` | Fixed. Both snap; four `transition-all` uses now name their properties |
| Icon tile stacked above heading | `FeatureCard` | Fixed. The icon sits in the top row in its accent colour with no tile; the title and sentence settle at the bottom of the card |
| Single font for everything | `index.html` | False positive. The detector reads only the first family in the combined Google Fonts URL; Spline Sans and Anonymous Pro both load |
| Side-tab borders | Blog pull quote (earlier build) | Fixed. The quote sits on the soft wash |
| Nested cards | Reference site example frames | Kept. A frame around a live example is the reference site's structure, not a page pattern |
| Purple gradients (AI palette) | Lavender in the signature gradient and aura tiles | Kept. It is the Health OS palette |

## 4. Taste

| Rule | Result | Action |
|---|---|---|
| No em dashes in visible copy | One remained, in the video placeholder hint | Fixed. None in rendered strings, the catalogue or the docs |
| At most one eyebrow per three sections | Every section had a group eyebrow | Fixed. Only the first section of each group shows it: 4 of 22 |
| No `window.addEventListener('scroll')` | `Nav` | Fixed. Uses Framer's `useScroll` |
| Grain only on fixed layers | Alternating sections used a blend-mode noise layer over tall scrolling sections | Fixed. The texture is a static background image; the paper grain stays a fixed, non-interactive layer |
| No numbered labels, no rows of identical feature cards | Pillars numbered 01 to 04 in a four-across row | Fixed. The heading holds a column beside a two-by-two grid; numbers removed |
| One label per intent | An action card repeated its button label as its title | Fixed. "Book the walkthrough" is the call and "Start the check" is the diagnostic, everywhere |
| Middle dot at most once per line | Blog byline and figure caption | Fixed. Author on its own line; a plain caption replaces joined tags |
| Readable text contrast | Labels and figures in `ink-400`, which the token reserves for placeholders and muted icons | Fixed. Moved to `ink-500` across widgets, the preview and the checklist |
| Narrow screens | Nav overflowed at 375px; preview rows wrapped onto three lines | Fixed. The nav button moves into the menu below `sm`; preview rows truncate and the chart tile hides |
| No div-built product screenshots | `DashboardPreview` in the command hero | Open decision: replace with a real, cropped screenshot |
| Hero top padding at most `pt-24` | Hero rule uses 176px from `md` | Kept. Tumai's hero spacing rule |
| Dark mode for consumer pages | No dark theme | Kept. Decision R2.2 |
| Lucide discouraged | Components use Lucide | Open decision: the filled icon set |
| Emoji discouraged | Badges and overview cards use emoji | Kept. Tumai asked to keep every badge |

## 5. Critique

Reviewed as the two users the system serves.

**An agent building a page.** It can now answer "which component, and may I use it here" from one entry: purpose, use, avoid, API and source, in the same words on the site and in `REFERENCE.md`. Values cannot drift because nothing is hand-synced. The remaining ambiguity is honest and listed: radius, the primary button colour, the audience noun, the archetype, icons and the logo files.

**Tumai reviewing the site.** The system reads calmer: one gradient moment per view, fewer colours, one heading size, cards with distinct jobs, widgets that move only when seen. Open decisions sit on the Checklist page with what happens until each is made.

**Still weak.**

- One very long page (23 sections). The rail and jump menu carry it, but separate routes would make sharing a section easier.
- The JavaScript ships as one 690 kB chunk (192 kB gzipped). Lazy-loading the image library and widgets would cut first load.
- The command hero's product preview is a coded mock until a real screenshot exists.
- A few widget samples use round numbers (50%). They are labelled sample data, but less tidy figures read more real.

## 6. Verification

- `npm run build`: tokens in sync, docs in sync, 96 files lint-clean, types clean, production build succeeds.
- Impeccable detector on `src/` and `dist/`: one known false positive.
- Screenshots of all 23 sections in the light theme at 1440px; hero, tokens, cards, widgets and the live page in the paper theme; hero, overview, tokens, buttons, cards, widgets, signature and the live page at 375px.
- No page errors or console errors in any capture.

## 7. Follow-up edits from Tumai

Five changes made after reviewing the live site.

| # | Edit | Result |
|---|---|---|
| 1 | Primary colours: Sunlit Apricot `#F5A060`, Expressive Rose `#E85BA8`, Neutral Lavender `#A668D9` | These are the 400 shades. Lavender moves from `#A666D9` to `#A668D9`, including the signature gradient |
| 2 | One button colour that works globally | Primary is Expressive Rose with white text on every ground. The carbon tone is removed from `Button` and `IconButton`, and every carbon button (nav, footer, action cards, widgets, command widget, social templates) now uses rose. Text buttons are ink on a rose underline |
| 3 | No 700 shades; each colour at full strength plus two lighter shades | Brand colours are 400, 200 and 50; status colours are 600, 300 and 100. Text that used a 700 shade is now ink, with colour carried by tints, edges, icons and dots. Badges gain a light same-hue edge. Widget tiles, avatars and funnel bars run from 400 into 200 with ink figures. Error messages use error-600 |
| 4 | Softer, gradual image transitions on every image card | One shared `image-fade-*` class: the photo stays whole for 35%, then falls away along an eased curve. Used by content, service, action and resource cards and the social story and link preview. Image library previews keep the whole image visible |
| 5 | Replace the warm sunrise gradient and calm the action card | Warm sunrise is removed. Soft dawn (white into apricot and rose tints) replaces it. The action card uses soft dawn or the soft wash, with a hairline edge, ink text and the rose button. The signature gradient is limited to the logo, a highlighted word and thin accents, never a large fill |

White text on Expressive Rose measures about 3.2:1. It is recorded as an open decision with the primary button colour.

## 8. Softer widgets and cards

Tumai asked for every widget and card to use a lighter tone, so nothing reads too poppy.

- `src/components/widgets/tones.ts` is now the one colour source for widgets: light (200) for bars, rings, tiles, chart marks and selected states, soft (50) for tile ends, bar starts and tints, and a light brand sweep for progress fills and gauges. No widget uses a full-strength shade for data.
- Stat and countdown tiles, avatars and funnel steps run 200 into 50 with ink text. Rings, gauges, ticks, the heatmap, bars, stars and progress fills use 200. Today, selected slots, finished steps and toggles use rose-200 with ink text.
- Cards: feature card icons, overview dots, seat bars and preview dots use 200. The featured pricing card has a rose-200 edge and soft check marks.
- Kept at full strength: primary buttons (including the plan card and timer controls), checked form controls, focus rings, and the green live dot, which reports a real state.

## 9. The button colour

Tumai set the standard button colour to `#F8C39C`, which is the apricot-200 token.

- Primary buttons and primary icon buttons are apricot-200 with dark ink text (about 10:1 contrast), on every ground including the carbon footer. Hover lightens the fill slightly.
- Text buttons underline in apricot-200.
- The same fill replaces rose on the command widget send button, the live timer, the plan card and the social story template, and replaces white on the footer Join button.
- The open decision on the rose button's contrast is closed. Checked form controls and focus rings stay rose-400.

## 10. The video player

Tumai supplied a video player component and set it as the only video UI style in the system.

- `src/components/ui/video-player.tsx` now holds that player. The video has clean rounded edges and nothing around it: the old hairline frame, soft-wash placeholder, native controls and corner mini-player are gone.
- Its styling uses tokens: the translucent `#111111` panels became `carbon/60` and `carbon/80`, `rounded-xl` and `rounded-2xl` became `rounded-lg`, and `text-sm` became the label role. The supplied shadcn `Button` was not copied in, because the system's own `Button` lives at the same path; the controls are small buttons inside the player.
- Added for use without a mouse: the control bar also shows on keyboard focus and while paused, the seek and volume tracks are keyboard sliders, and every control has a label. Play state follows the video's own events, and the duration shows before playback starts.
- `overview.mp4` itself has a pastel frame recorded into every frame (25px at the sides and 57px top and bottom at 1920 × 1080) and a stray dot in its first frame. The player's new `crop` option trims those edges on screen (6% top and bottom, 2.6% each side), and an edge scan across nine points in the video found no frame pixels left. Re-exporting the file without the frame would make the crop unnecessary.
- The blurred control bar is recorded as a deliberate departure from the no-glass direction.

## 11. Round 6: headlines, one accent and new parts

Tumai's ten edits of 14 September 2026.

| # | Edit | Result |
|---|---|---|
| 1 | H1s in two tones with pictures between the words | Every H1 is a `Headline`: the point in ink-900, the rest in ink-400, and two or three square photoreal tiles written into the copy as `{id}`. Headline tokens, a Headlines section, a six-tile library and the tile recipe. Tiles were generated with GPT Image 2 through KIE at 6 credits each, after Tumai approved the cost |
| 2 | Soft orange for every button, tick, selected state and link | Apricot replaces rose on checked controls, selected states, the active nav item, tabs, command chips and link underlines. Lint rejects rose or lavender interaction states |
| 3 | No carbon backgrounds | The carbon logo ground, carbon ticker tone and badge demo are gone; the footer and tooltip are light. Lint rejects a solid `bg-carbon` |
| 4 | Long logo variations | `LogoLong` in white and filled background, shown on light and paper |
| 5 | Remove three experimental motions | BorderGlow, PointerSpotlight and GradientShimmer removed |
| 6 | Overview cards in the supplied card style | `CarouselCard` on a marquee drifting left to right: photos, one figure and an Open action |
| 7 | Blog without the filled quote, and a better blog system | A pull quote on a rule, table of contents, author note, related posts, featured post, filters and pages. Relume was not connected, so the anatomy came from Relume's public component pages |
| 8 | Three more card types from 21st.dev | Session, profile and steps cards. 21st.dev was not connected, so they were rebuilt from its public pages |
| 9 | Delete the docs template | Section and entry removed |
| 10 | An interactive bento from the supplied component | `FeatureBento` in photo, tint and quiet styles |

## 12. Audit and critique of round 6, and the fixes

An Impeccable audit and critique ran on the round 6 changes, with a design review and a detector plus browser pass kept apart. Scores before fixes: audit 13/20, critique 24/36 (heuristic 9 not applicable). The CLI detector found nothing in the changed files; the browser scan's findings were mostly false positives or older code (label tracking, the dashboard preview's nested cards).

Tumai chose to fix every P1 and P2, to draw focus in ink, and to keep apricot for interaction only.

| Priority | Finding | Fix |
|---|---|---|
| P1 | The overview marquee exposed 132 tab stops, half inside its aria-hidden copy; focus landed off-screen and kept moving; no pause | The duplicate copy is `inert`. The row pauses on focus and hover, brings a focused card into view, and shows a pause button (`pauseControl`). Cards use `photoControls="pointer"`, so each is one keyboard stop named "Open {title}" |
| P1 | Article text clipped on phones: a 422px column in a 292px article | `ArticleLayout` gives both columns `min-w-0`; the table scrolls inside its frame |
| P1 | The apricot-400 focus ring measured 2.08:1 on white and looked like a selection | Every focus ring and focused field border is ink-900. Lint rejects apricot focus rings |
| P2 | Apricot's selected look was used as decoration | The bento highlight cell, headline tiles, In studio and Featured badges, the featured pricing card, steps nodes, comparison column, ticker icons and token demos move to rose, lavender or neutral |
| P2 | The bento had four heading-size elements and broke at tablet width | One large figure; the hero title and facts use the subheading size. Two columns on tablets, three from lg. Only the action cell lifts; the arrow no longer squashes |
| P2 | Tiles could start a line away from their word | Each tile is kept on the line with the words either side |
| P2 | Carousel arrows hidden on touch and small dot targets | Arrows show on touch screens; dots have 24px targets |
| P3 | Cards that are not links lifted on hover; the blog grid reread every card on filter change; the copy toast claimed success without checking | Session, profile and carousel cards no longer lift; the blog index announces a result count; the copy action reports failure |

Also: the category filter wraps instead of hiding chips, the session card's action is a text button with an add-to-calendar icon button, the footer's Join button is secondary, the blog has six sample posts so pages are even, and sample people keep one role each.

Kept as recorded departures: the ink-400 rest of a headline (2.74:1 at heading size) and the 0.06em label tracking.

## 13. Round 7: the accent word, libraries and new page parts

Tumai's requests on 15 September 2026, and what changed.

| # | Request | Result |
|---|---|---|
| 1 | Every headline word black except one descriptive word in #F8C39C, with its squircle beside it | `Headline` now takes one string: `[word]` marks the apricot-200 accent word and `{id}` marks tiles, up to three, one beside the accent word. The ink-400 rest is gone. The accent colour is 1.6:1 on white and recorded as Tumai's departure |
| 2 | A collapsible library of 20 headlines, using the copywriting skill | `HEADLINE_LIBRARY`: 20 lines mapped to the database's messaging pillars, each marked verified, adapted or new, with copy buttons and a pillar filter. It opens on four lines |
| 3 | A collapsible squircle library made with KIE, modelled on Tumai's squircles | `HEADLINE_TILE_LIST`: 80 tiles covering all 107 of Tumai's words as search tags, grouped, with a picture brief each. 6 are ready. **Generation is paused**: Tumai will supply example squircles first, so no credits were spent |
| 4 | Tag the tile library with the headline words so it can be searched | Search, group filters and a ready or to-make filter. Journey, wellbeing and heal are tags only, with the database rule shown on the tile |
| 5 | The video follows the reader into the corner, tokenised | `VideoPlayer` floats bottom right while playing out of view; size, inset, threshold and layer from `tokens.json → video` |
| 6 | Only the gradient banner | `Ticker` has one tone, the soft wash, on both themes; `tokens.json → banner` |
| 7 | Fix the badges | Sentence case, no emoji, flat tints, no apricot, a dot for real states, and one colour key for every set |
| 8 | Lead magnets from 21st.dev | `Ebook` (3D book opening into a react-pageflip reader with an email capture) and `SwipeFiles` (a card fan rebuilt on Framer Motion, cards turn over to a spec sheet) |
| 9 | Pricing movement and a celebration in brand colours, keeping the design | Cards rise in on scroll with the featured plan forward; a billing switch with brand-colour confetti (`useCelebrate`, `tokens.json → celebration`). The database records no annual plan, so the Health OS table has no switch and a sample studio table shows it |
| 10 | FAQ with an orange plus that becomes a cross, an image beside the answer, about 800px wide | `Faq` at `max-w-narrow` (800px); Disclosure uses the same plus |
| 11 | More flexible feature components from 21st.dev | `FeatureSteps`, `FeatureTabs`, `FeatureGrid`, `FeatureCards` (outline or muted); the steps hold time is `motion.autoplay.step` |
| 12 | Three bento systems | Feature bento (existing), `ProductBento` (drawn product visuals) and `GalleryBento` (a studio or practice page) |

Found on the way: the 407px phone overflow blamed on the funnel widget last round was a screen-reader table caption escaping its scroll frame. Table wrappers are now positioned, and the page is 390px wide at 390.

## 14. Audit and critique of round 7, and the fixes

Impeccable ran again with the design review and the detector pass kept apart. Critique 28/40, audit 15/20. The CLI detector found nothing in the changed files; the browser scan confirmed low contrast on tinted labels and flagged mostly false positives (the reference's example frames as nested cards, fixed-height buttons as cramped). Tumai had asked for everything changed to be polished, so every P1 and P2 was fixed.

| Priority | Finding | Fix |
|---|---|---|
| P1 | A headline could use a tile still to be made and ship a blank squircle | `Headline` warns in development; library rows show "tiles to make"; copying such a line warns; lines whose tiles are made come first |
| P1 | Small grey labels at 3.7:1 on rose and lavender tints and 4.29:1 on grey bars | ink-600 on tints, frame labels and unselected segments. Contrast checks pass on every changed section |
| P2 | Apricot focus rings on search and form fields | Inputs, selects, textareas, the command bar and every search field focus in ink. Lint rejects an apricot focus ring or border |
| P2 | The video on phones: the control bar overflowed, 36px floating buttons, 4px slider hit areas | 44px floating buttons, taller slider hit areas, and one speed button below `sm` with the volume slider hidden |
| P2 | Badge colours meant different things in each set | One key everywhere: rose for people and in person, lavender for online and self-paced, neutral for everything else, outline for quiet, status colours with a dot. The key is shown on the page |
| P2 | The banner's pause icon collided with the scrolling text | A solid backing behind the control |
| P3 | Eyebrows on every feature demo; the feature steps began before their pause control was visible; small filter and copy targets; "Feature grid" named two things | One eyebrow; steps play only when their photo is mostly on screen; 34 to 36px targets; the Blocks example is now "Pillars" |

Kept as recorded departures: the apricot-200 headline accent word, and planned tiles shown as dashed placeholders until Tumai's examples arrive and generation runs.

## 15. The icon library and Tumai's headline mock-ups

On 15 September Tumai supplied headline and tile mock-ups (Health OS Branding Guidelines, pages 11 and 12) and approved KIE generation of the full library.

| Change | Result |
|---|---|
| Headline accent colour | Sampled from the mock-ups as #F4A060, so the accent word is apricot-400 `#F5A060` (about 2.1:1 on white, recorded as a departure). The accent word no longer needs its own tile; tiles sit right after the words they picture |
| Tile style | Every tile is a photoreal object on a warm charcoal squircle, a radial ground sampled from the mock-ups (`tokens.json → icon.ground`). It is the one small dark surface in the system |
| One icon library | The empty Icons section and the squircle library merged into the Icon library (section id `icons`). `IconTile` shows an icon on its own at 40, 64 or 96px; every icon has a 1024px PNG, a 320px WebP and a baked tile PNG for Canva. Controls keep Lucide line icons |
| Full library generated | 74 new icons with GPT Image 2 through KIE, after a three-icon pilot: 462 credits (about $2.31) including three redos for a numbered watch dial, a gauge that read as a clock and an unclear pottery wheel. All 80 icons are ready, covering all 107 headline words |
| Headlines | The library and examples follow the mock-ups: "You built {blocks} it. Now [make] it run {computer} without you." and "Fewer {tools} tools, wired {cables} [together]" |

## 16. Round 8: ten requests, 148 new icons, and a whole-system audit

Tumai's ten requests of 15 September 2026, then a follow-up on the home hero, and what changed.

| # | Request | Result |
|---|---|---|
| 1 | Blend above and below the fold | The home hero has no hairline at its foot. `Hero fade` dissolves its ground into the textured video section over `space.hero-fade` (18rem) on an eased curve, and the next section's grain fades in over the same height |
| 2 | New hero H1 and subtitle | First "The design system behind the #1 platform in the health industry", then, in Tumai's follow-up, "The [ultimate] design system for Health OS" with the subtitle "Be cool and use the design system for all things Health OS." The ranking claim is gone |
| 3 | Real confetti for celebration moments | `celebrate.tsx` rewritten: curly streamers, short ribbons, stars and dots pop from the top edge of the control and drift down in under two seconds, in brand 400 and 200 (`tokens.json → celebration`). One self-mounting layer; `<Button celebrate>` or `celebrate(element)`. On the annual switch, the featured plan, booking and confirm buttons, a save once it finishes, the ebook submit, swipe files, the first play of a video and a Celebrate demo in Motion |
| 4 | Wider overview viewport | The overview marquee runs edge to edge of the page area (`Section bleed`); the pause button stays on the content column; the marquee mask is capped at 5rem |
| 5 | Quieter FAQ and disclosure | A new `text-title` role (18px Spline Sans 500) for questions and disclosure titles, and a 24px plus. Two families, five roles |
| 6 | One pricing system | Every pricing view is a `PricingTable`: the Cards section holds the one reference, Blocks no longer duplicates it, and the in-product plan widget is the same card. Tumai confirmed annual prices exist: monthly × 10, two months free ($970, $2,970, $4,970 AUD a year) |
| 7 | New headline system everywhere | Social post headlines use `Headline`; stale "two-tone" copy and the overview card updated; section h2s stay plain by rule |
| 8 | Floating, leaning tiles | Inline tiles lean ±3° alternating and float 0.06em over 6s (`headline.tile-tilt`, `float-*`), paused off screen and with reduced motion. The home hero instead floats eight tiles around the headline (`FloatingTiles`, `icon.float-*`) |
| 9 | Hyper-realistic icons | 148 new icons with GPT Image 2 through KIE: 105 objects from Tumai's reference sheets (8 already in the library skipped; brand look-alikes made generic, no numbers or letters) and 43 for software, email, automation and programming. 888 credits (US$4.44), no redos. 228 icons in 17 groups |
| 10 | Impeccable audit, critique and polish | Impeccable updated from 4.1.1 to 4.3.1, then run as below |

### Critique and audit

Impeccable 4.3.1, with the design review and the detector and browser pass run as separate agents. Critique 28/40, audit 14/20 (accessibility 3, performance 2, responsive 3, theming 3, integrity 3). The CLI detector found 0 findings in `src` (verified against a planted file); the browser overlay's 1,052 findings were almost all recorded departures (label tracking, example frames read as nested cards, image hovers inside links).

| Priority | Finding | Fix |
|---|---|---|
| P1 | The hero search took typing and did nothing; "switch the theme" scrolled away | The search matches the query against every section and catalogue entry and jumps to the best one, with a toast either way; `/` focuses it; the theme chip switches the theme; toolbar icons never take focus |
| P1 | 12px ink-500 labels at 3.66 to 4.29:1 on paper, textured, surface-2 and tinted grounds (185+ in light, 272 in paper) | ink-500 darkened from #7C746B to #6E665D: at least 4.5:1 on every neutral and 50 ground in both themes. Placeholders move from ink-400 to ink-500 |
| P1 | Confetti defaulted on for clicks that complete nothing | Session, profile and action cards opt in; a Button link that leaves the page never celebrates (warns in development); the save demo celebrates only after it finishes |
| P2 | Hard to find things: a 233k px phone page, unpaged image sections, a screen of icon chips, a rail that lost the active item | Backgrounds open on a peek (6 wide, 8 tall, 4 crops) and the image library on 12, each with Show all; icon chips scroll sideways on phones; the rail keeps the current section in view |
| P2 | Weight: 17.8 MB of full-size card PNGs at 220px; one 919 KB chunk | Card crops use 800px JPEG thumbnails (576 KB, originals still download); React, Framer Motion, Lucide and react-pageflip split into their own chunks; below-the-fold images lazy |
| P2 | The annual switch reflowed the pricing row | Figure and cadence on their own lines, so heights and buttons stay put; each card says "Billed annually, 2 months free"; the live region reads every new price |
| P2 | Calendar days 19px wide at tablet width | Widget thirds are halves until xl |
| P2 | Docs drift | The headline examples no longer show the old hero; Motion counters labelled for product UI and real start times |
| P3 | Clipped filename notes, a headline tile spilling in the phone link preview, FAQ aria-controls to a missing panel, focus ring flush on FAQ text, hard-coded colours in two widgets, "Move", a black video before load | Notes wrap; the preview headline uses the subheading size on phones; aria-controls only when open and padded rows; palette constants; "Replay"; a poster frame from the video |

Kept as recorded departures: the floating home hero tiles and leaning inline tiles, confetti at 400. Still open at P3: most `focus-visible:ring-2` lines lack `ring-offset-2`, the 194 KB logo PNG shown at 32px, three tickers on the reference page.

## 17. Hero field, audio and music

Tumai's follow-up of 15 September 2026.

| Request | Result |
|---|---|
| Hero copy and floating tiles from a supplied React example | "The Ultimate Design System / For Health OS", title case on two lines and centred (Tumai's copy; `\n` now breaks a Headline line). `FloatingTiles` rebuilt from the example: tiles rise in with a stagger, drift on their own 5 to 10 second loop (8px up and down, 6px side to side, 5° rock) and ease up to 50px away from a mouse within 150px on a spring (stiffness 300, damping 20), all from `tokens.json → icon`. Tumai's nine icons: cloud, computer, monstera, terminal, paper plane, feather, lightning bolt, diamond, orange juice. Nine on wide screens, six on tablets, three on phones; above the hero fade, clear of the words, search and chips at 390, 768, 1024, 1280, 1440 and 1920. Mouse only, stops off screen and with reduced motion |
| Audio on the blog template | `AudioPlayer`: icon tile, title and source line, play with 15 second skips, seek bar, elapsed and total time, speed and mute, with loading, error and replay states and a transcript slot. The sample is a 1 min 52 sec clip (1:08 to 3:00) of Hustle HQ season 1, episode 3, "Aroma with Audrey", Health OS's own episode: downloaded once with yt-dlp (run through uv) and cut in Chrome to a 1.3 MB AAC file in `public/media/audio` |
| A Health OS music section | New section `music`: nine music cards on a grid of three. The nine songs are other artists' copyrighted recordings, so they are referenced, never copied: each plays from its official video through YouTube's own player in the card artwork (its terms require the player to stay visible), with Health OS previous, play, next and seek controls, one song at a time. Recorded as Tumai's exception to the one-player rule |
| Onboarding on yearly plans | Tumai confirmed the $997 applies to yearly plans too; db-health-os updated (D47, D48 narrowed to renewal and trial conversion) |
| Tiles tighter, scattered, behind the H1; search lower | Tumai marked the area on a screenshot. `FloatingTiles anchor="headline"` now sits at the headline's top centre, behind its letters, with x, y and size in the headline's em, so the field hugs the words the same way at every width. Each tile has its own size (about 44 to 110px at full heading size, `icon.float-size-min` and `float-size-max`) at uneven positions around the headline and subtitle only, eased just clear of the glyphs so no letter loses its edge. A new `space.hero-gap-lg` (12rem) between the subtitle and the search from md up puts the search at 64 to 78% of the visible hero at 1440 × 900. Checked at 390, 768, 1024, 1280, 1440 and 1920: no tile over the subtitle, near the search, clipped or touching another |
| Headline spacing, logo, fade, edge tiles, soft wash submit | The hero headline's two lines now sit at 1.32 line height (77px apart at full size, from 63px). The words Health OS are the long filled logo (`{@logo}`, alt "Health OS"). Each line fades from ink-900 to `rgba(31, 31, 31, 0.62)` like Tumai's 21st.dev example, with the accent word solid. Every tile sits at least 0.41em from the letters at all six widths. Bonsai, horseshoe magnet and rope knot float small at the hero's far edges from md up. The command widget's submit disc is the soft wash. Gradient text and a gradient button are recorded as Tumai's exceptions |
| Hero layout pass | The words Health OS are back in dark type (the logo came out of the headline). The orange juice tile is gone. The computer is smaller. The monstera and lightning bolt moved down to the bottom left of the hero, the horseshoe magnet sits alone in the top right corner, and the diamond sits alone above the search towards the middle, all in the hero-anchored field. Checked at 390, 768, 1024, 1280, 1440 and 1920: no tile near the search or chips, clipped or crowding another, and headline tiles at least 0.35em from the text through their float |

## 18. One icon system in every card, and light grounds

Tumai's request of 15 September 2026, with a screenshot of the pillar cards.

| Request | Result |
|---|---|
| Card icons don't match the icon library | Every card, feature, bento and widget tile that pictured something with a Lucide line icon now uses `IconTile` from the library: the pillars (cables, quartz, shield, metronome), `FeatureCard`, `FeatureCards`, `FeatureGrid`, `FeatureSteps`, `FeatureTabs`, `FeatureBento` (highlight and feature), the aura stat tiles and the bento data. Icons are tile ids everywhere (`icon: 'desk-calendar'`), so a line icon can no longer slip into a card. Swept the rest of the system: line icons remain only on controls and small labels (arrows, close, ticks, play, badge and metadata icons, gallery detail labels, ticker statements, the dashboard preview), as rule 15 already said |
| Black tiles clash beside faded photos | Two light grounds join charcoal, from the same cut-outs, so no KIE credits were spent: paper (warm ivory radial, `icon.ground-paper`) and white (`icon.ground-white`), each with a hairline edge (`icon.edge-light`) and a soft contact shadow under the object (`icon.object-shadow-light`) so pale objects such as the feather, cable and paper plane still read. `IconTile ground: carbon | paper | white`, one ground per component: charcoal on plain cards, paper beside dissolving photos and on tabs (feature steps, feature tabs, quiet bento), white on washes, tints and gradient tiles (photo and tint bentos, stat tiles, muted feature cards). A new xs size (28px) sits beside tab labels |
| A background switch in the icon library | The library has a Charcoal, Paper and White switch above the list, remembered in the browser. Download follows the switch: charcoal gives the baked file; paper and white are baked in the browser at 512px with the same squircle, object size and edge (`src/lib/bake-tile.ts`, colours generated into `ICON_GROUNDS` from the tokens), saved as `<id>-tile-paper.png` or `<id>-tile-white.png`. The Icon tile example now shows the three grounds, a paper tile beside a dissolving photo and a white tile on the soft wash |
| Checks | Types, tokens, docs and lint pass. Checked at 1440 and 390: no console errors, no broken tile images, paper and white downloads open at 512px with the right ground |

## 19. The email and newsletter library

Tumai's request of 15 September 2026: the emails Health OS will send long term, inside the design system, searchable and compact rather than one long scroll. Tumai chose the series (newsletter, lead nurture, cold and warm outreach, onboarding, retention and renewal, win-back, billing, account and trial; no templates for operators' own clients) and approved 48 KIE credits for newsletter header images.

| Part | Result |
|---|---|
| Research | db-health-os first: the email, newsletter, cold, warm, funnels, capture, onboarding, retention and reactivation playbooks, banned language, brand voice, pillars, pricing (yearly and onboarding), guarantees and trial, production layer, agents and AI disclosure, and the subject line and CTA banks. benai-skills cloned over SSH for the email-sequence and newsletter-writer skills, which set the sequence shape; the database overrode them wherever they differed (no social proof email, no discount, no urgency, no question-mark subjects) |
| 46 emails in 10 series | Newsletter (4 editions, one idea each, 400 to 460 words, with an SEO title, description, slug, search phrases and alt text for the web version); lead nurture for the Founder-Dependence Score, 8-Tool Cost Audit and Steady-Week Check (the database's day 0, 3, 10 and 21 spine, 12 emails); cold (4, a researched opener, reply asks); warm (4, one person at a time); onboarding (7, from payment to handover); retention and renewal (5); win-back (3); billing, account and trial (7). Written as data, each mapped to a pillar where it carries an argument, each listing the open decisions it depends on |
| Honest gaps | Sending is blocked (no list). Diagnostics not built (D11), guarantee clock (D45), what "we keep working" commits to (D6), day 91 (D46), yearly renewal (D48), churn reasons (D16), the trial cancellation path, from-name, kickoff link, ABN (so receipts are receipts, not tax invoices) and healthos.au are shown as open, never guessed. Emails that cannot run without them say so |
| Checks | A script checks every email against its playbook word range and for dashes, exclamation marks, emojis, banned words, subject rules, AUD and the usage note beside prices, one button, and undefined merge fields: all 46 pass |
| The section | `emails` in Applied: search across subjects, bodies and open decisions; All, Marketing or Customers; series fold open and closed in a list beside the email; steps as buttons; subject, preview, send moment, ask, word count and footer reason; the email rendered in a frame exactly as the inbox shows it, desktop or phone, samples or merge tags; copy the subject, HTML or text; open decisions, merge fields and the web version in disclosures. `?email=id` opens one email. On phones the list folds behind a Browse button |
| Images | 8 GPT Image 2 renders (48 credits), the first option of each chosen: a rotary telephone at a reception desk, eight ribboned keys, an hourglass on a shelf, a pen on a note card, cropped to 1200 × 600 JPG. The onboarding welcome uses a library photo; account emails use icon tiles baked on white. Raw renders, prompts and the task ledger are in `health-os-ds-rebuild/evidence/newsletter-2026-09-15/` |
| Navigation simplified | Tumai crossed out the series list beside the email. All navigation now sits in one bar across the top: the search (matches drop down under the field; Enter opens the first), the ten series as chips in a Marketing row and a Customers row, and the emails in the chosen series as chips with previous and next. The email gets the full width below it. On phones each chip row scrolls sideways and keeps the chosen chip in view |

## 20. The dashboard library

Tumai supplied 29 Health OS product screens on 17 September 2026 as 4K stills, with screen 79 as the main one.

| Part | Result |
|---|---|
| Read and tagged | Every screen was read, then tagged with the part of the product it belongs to (overview, bookings, clients, messages, funnels and websites, content, automations, courses and community), its layout (overview, list and table, calendar, builder canvas, feed, split preview, template gallery), the parts it is built from (23 in all, from KPI tiles to kanban boards), how it feels (palette and density), what to take from it, the words that name the screen, and every departure from the system |
| Departures recorded, not copied | 26 of the 29 carry at least one: a saturated pink or magenta button where the system has one apricot button colour, gradient tiles, teal badges, a near-black button, and in one community screen, post copy making health claims. Three screens carry none (84, 86 and 90), so they can be built from the tokens as they stand. The rule the section states plainly: take the shape from the picture and the paint from the tokens |
| The section | `dashboards` in Applied: the featured screen opens large with its area, layout, feel, parts, what to take and its departures, then a search across names, parts and departures, chips for the eight areas, and a grid of every screen that opens nine and shows the rest on request. Picking a screen moves it into the viewer. Each screen links a 1920px copy and the 4K original |
| Files | `public/dashboards/<id>.png` as supplied, plus a 1920px JPEG in `large/` for the page and a 960px JPEG in `thumbs/` for the grid. 23 MB in all |
| Checks | Types, tokens, docs and lint pass. Checked at 1440 and 390: no console errors, no broken images, no sideways scroll |

## 21. The floating card on the main dashboard

Tumai asked on 17 September 2026 for the main dashboard to float and tilt with the pointer, then turn over and play a video, with the code portable to other sites. The effect came from the Fitness OS hero card Tumai supplied.

| Part | Result |
|---|---|
| Tokenised first | A new `card3d` group in tokens.json: perspective, tilt, the resting angles, how long it follows and turns, the lift and the glow. It generates the CSS variables and a `CARD_3D` export, so the React component, the CSS and the copy-ready snippet all read the same values. The lift is ink rather than black and the glow is apricot-400, which is the one place the brand colour is a glow |
| The component | `Card3D` (`src/components/ui/card-3d.tsx`): front, back, the words on each button, tilt, and a controlled `flipped` if a page wants to drive it. The front is the click target so the back keeps its own controls; the way back sits under the card, clear of the video, and Escape does the same. Turning over squares the card up to the screen, level and even, and scrolls it to the middle of the view, so the video is watched flat (Tumai, 17 September 2026). The back is only mounted while it shows, which stops the video the moment the card turns back |
| The video behind it | `VideoPlayer` gained `playOnReveal`: it plays as soon as a click reveals it, and falls back to muted if the browser refuses the sound. Never on page load |
| Honest degradation | The tilt follows a mouse only, so a touch simply turns the card. With reduced motion on, the card sits flat and the faces cross-fade. The hidden face takes no clicks, so nothing behind the card can be pressed by accident |
| Portable | The section carries the whole effect as plain HTML, CSS and JavaScript with a copy button: eight variables at the top, no build step, nothing Health OS specific inside it |
| Checks | Types, tokens, docs and lint pass. Checked at 1440 and 390 and with reduced motion: the card rests at an angle, tilts to the pointer, turns over, plays with sound at 1x, turns back on the button and on Escape, and the video unmounts. No console errors, no sideways scroll |


## 22. Case study and testimonials (17 September 2026)

Tumai asked for a case study and thirty testimonial cards on two moving walls, built from the case study playbook in benai-skills and from two components he supplied.

| Question | Answer |
|---|---|
| The rule it runs into | The system said "no testimonials" and the database blocks every client result, count, quote and logo (C21): nothing has been measured and nobody has given permission. Tumai asked for the design, not for publication, so the components were built and everything in them is marked. The standing notice opens the section, every card carries a Sample mark, the case study's every figure carries the evidence it would need, and `SAMPLE_NOTICE` travels with the data. VOICE.md, the checklist and CLAUDE.md rule 18 now say the same thing in the same words |
| The walls | `TestimonialWall`: fifteen short quotes on three rows that drift sideways, the middle one reversed, each row slower than the last. Built on the existing `Marquee`, which gained one prop (`paused`) so a single button holds all three rows rather than each row carrying its own. `TestimonialColumns`: fifteen longer quotes on three columns that drift upward at different speeds, columns falling away to one on a phone. Both sized and paced from a new `wall` token group |
| The card | `TestimonialCard`: the quote leads, the person follows. Two sizes, the row card fixed to the token width and the tall card filling its column. The Sample mark sits where a logo would go |
| The case study | `CaseStudy`: who they are, what was wrong, what we set out to do, what was done, what changed, what they said, what happens next. The actions are the real offer (30-day setup, three agents, the wellness funnels, handover to the team); only the results claim an outcome, and each one lists what it would need before it could be published |
| The words | Thirty quotes and one story written in the primary buyer's voice and mapped one to one onto the three messaging pillars. No result figures, no health claims, no earnings, no regulatory status. Personal detail (twelve years, three staff) rather than performance numbers |
| The faces | Thirty generated portraits, 180 KIE credits approved by Tumai, on the same warm paper ground as the icon tiles so the set reads as one. They picture nobody real, they only appear beside a Sample mark, and the recipe is in ASSET-RECIPES.md §9b |
| Checks | Types, tokens, docs and lint pass. Checked at 1440 and 390 and with reduced motion: both walls drift, both stop on their button and on hover, the columns become a scrolling list with reduced motion on, one column on a phone, no console errors and no sideways scroll |
